// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  // remove the url property entirely
}

model Book {
  id    String @id @default(cuid())
  title String
  link  String
  img   String
}